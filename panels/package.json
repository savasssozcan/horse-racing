{
  "name": "horse-racing-panels",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build && concurrently \"vite build --watch\" \"vite preview --port 5001\"",
    "build": "vite build",
    "preview": "vite preview --port 5001",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "overlayscrollbars": "^2.0.2",
    "overlayscrollbars-vue": "^0.5.9",
    "vue": "^3.4.0",
    "vuex": "^4.1.0"
  },
  "devDependencies": {
    "@originjs/vite-plugin-federation": "^1.3.4",
    "@vitejs/plugin-vue": "^5.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "@vue/test-utils": "^2.4.4",
    "concurrently": "^8.2.2",
    "jsdom": "^24.0.0",
    "vite": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
