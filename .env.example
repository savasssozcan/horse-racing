# Optional: used when building host for production (e.g. Vercel).
# Leave unset for local dev (host uses localhost:5001 and 5002).

# Full URL to panels remote entry (after deploying panels)
# VITE_REMOTE_PANELS_URL=https://your-panels.vercel.app/assets/remoteEntry.js

# Full URL to race-track remote entry (after deploying race-track)
# VITE_REMOTE_RACE_TRACK_URL=https://your-race-track.vercel.app/assets/remoteEntry.js
